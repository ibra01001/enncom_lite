version: '3.9'

services:
  redis:
    image: redis:alpine

  backend:
    build: ./backend
    ports:
      - "5000:5000"

    depends_on:
      - redis

  frontend:
    build: ./App
    ports:
      - "5173:5173"

    depends_on:
      - backend